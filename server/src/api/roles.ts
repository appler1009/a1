import { FastifyInstance } from 'fastify';

export async function registerRoleRoutes(_fastify: FastifyInstance): Promise<void> {
  // Role routes are handled in the main index.ts file
  // This file is kept for backwards compatibility
}
