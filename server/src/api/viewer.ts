import { FastifyInstance } from 'fastify';

export async function registerViewerRoutes(_fastify: FastifyInstance): Promise<void> {
  // Viewer routes are handled in the main index.ts file
  // This file is kept for backwards compatibility
}
