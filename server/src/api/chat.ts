import { FastifyInstance } from 'fastify';

export async function registerChatRoutes(_fastify: FastifyInstance): Promise<void> {
  // Chat routes are handled in the main index.ts file
  // This file is kept for backwards compatibility
}
